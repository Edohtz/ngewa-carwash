# Ngewa Car Wash — Handoff Notes (edoh.md)Date: 2026-06-20This file summarizes the changes made, what's verified, what's still **NOT** done,and a checklist before/after pushing to GitHub. Read the 🔴 section first.\---## 🔴 DO THIS BEFORE ANYTHING ELSE (not code — but critical)The repo is **public** and leaked live credentials. These are compromised andmust be rotated **now**, regardless of code changes:* \[ ] **Firebase Admin private key** → Firebase Console → Project Settings →Service Accounts → *Generate new private key* (and revoke the old one).* \[ ] **Google Cloud Vision API key** → GCP Console → APIs \& Services →Credentials → delete \& recreate. Check billing for abuse.* \[ ] **Gmail app password** for `edmond2tz@gmail.com` → Google Account →Security → App passwords → revoke \& recreate.* \[ ] **JWT\_SECRET, ADMIN\_PASSWORD, all worker passwords** → set new values in`.env` (the server now refuses to start without `JWT\\\\\\\\\\\\\\\_SECRET`).Why urgent: anyone who viewed the public repo already has the old keys.### `.env` and `node\\\\\\\\\\\\\\\_modules` are still committed to git* `.env` (with secrets) is tracked and lives in git **history**.* `node\\\\\\\\\\\\\\\_modules/` (12,187 files) is tracked — bloats the repo.* Root cause: `.gitignore` was saved as **UTF-16**, so git ignored all its rules.To fix (do this before/with your next push):```bash# 1. Rewrite .gitignore as UTF-8printf 'node\\\\\\\\\\\\\\\_modules/\\\\\\\\\\\\\\\\n\\\\\\\\\\\\\\\*.db\\\\\\\\\\\\\\\\n.env\\\\\\\\\\\\\\\\n' > .gitignore# 2. Stop tracking them (keeps the files on disk)git rm -r --cached .env node\\\\\\\\\\\\\\\_modules# 3. Commitgit add .gitignoregit commit -m "Stop tracking .env and node\\\\\\\\\\\\\\\_modules; fix .gitignore encoding"```This stops *future* leaks. To scrub `.env` from **past** history you also need`git filter-repo` or BFG (ask if you want a walkthrough) — and rotate keys (above)either way, since the old commits are already public.\---## ✅ What was changed (all code-verified: `node --check` + script parse pass)### Finance / money correctness* POS accessory sales now count as **revenue**, not expenses (they were wronglywritten to the `expenses` collection, dragging profit down).* Revenue counts **approved washes only** (per your decision); POS always counts.* Revenue totals are **uncapped** (were silently limited to the last 500 records)and consistent across the dashboard, stats, and finance tab.### Security* All user text escaped before going into the DOM (**stored XSS** fixed, 47 spots).* Passwords hashed with **bcrypt** + constant-time compare on login.* Server **fails fast** if `JWT\\\\\\\\\\\\\\\_SECRET` is unset; removed guessable fallbacks.* **CORS** locked to same-origin (set `CORS\\\\\\\\\\\\\\\_ORIGIN` to opt specific origins in).* **helmet** enabled (CSP off to keep the inline-heavy UI working).* `trust proxy` set so rate limiting works behind Render/Railway.* **Rate limits**: login (20/min), OCR scan (30/min — protects Google billing),global API (600/min). Crash guards + JSON error handler added.### POS* Checkout wrapped in a **Firestore transaction** (no oversell race).* Totals computed from **catalog prices**, not the client payload (anti-tamper).* New `GET /api/pos/low-stock` + low-stock warning shown at checkout.### UI / UX* Worker performance = tappable **profile cards** (avatar, role, Active/Offline,metrics, performance bar). Opens the existing detail dashboard. No data/logic changed.* Dashboard **Install App (PWA)** card — version, last-updated, native install;auto-hides when already installed. (There is **no Android APK** — this is a PWA.)* Service worker: HTML network-first (fresh after deploy), one-line `VERSION` bump.### Search* `/api/search` now uses bounded **prefix queries** instead of scanning the wholecustomers collection. ⚠️ Behavior change: matches by **prefix**, not substring.### New file* `scripts/cleanup-pos-expenses.js` — one-time, **dry-run by default**. Deletes theold POS-sale rows wrongly stored in `expenses` (each has a `posRefId`).Run AFTER rotating keys: `node scripts/cleanup-pos-expenses.js` (preview),then `--confirm` to delete.\---## ⚠️ NOT done / could not be done here* **App was never run live.** I have no valid Firebase credentials in thisenvironment, so all verification is static (syntax + logic reading). You mustsmoke-test locally once you have fresh `.env` values.* **Git history scrub** of `.env` (see 🔴 above).* **Firestore backups + uptime monitoring** are console/CLI config (commands below).\---## 🧪 Manual test checklist (after fresh .env, `npm install`, `npm start`)* \[ ] Login as admin and as a worker (e.g. NGOSHA).* \[ ] Bad password → clean "si sahihi" error (not a 500).* \[ ] Check in a customer; confirm SMS/email path works (or fails gracefully).* \[ ] Plate scanner (camera) returns a plate.* \[ ] Dashboard: worker profile cards render; tapping one opens the detail view.* \[ ] Install App card shows version/date; "Sakinisha" prompts install on Android.* \[ ] POS: make a sale → stock drops, low-stock warning appears at threshold,sale shows as **revenue** in Finance (not expense).* \[ ] Finance tab: "Yote" total looks right; only approved washes counted.* \[ ] Search by start of plate / name / phone returns matches.* \[ ] Visit `/health` → `{"status":"ok"}`.## 🔎 Things to verify after deploy* \[ ] Set all env vars in Railway/Render (especially `JWT\\\\\\\\\\\\\\\_SECRET`).* \[ ] Health check path `/health` configured in the platform.* \[ ] Login rate limiter works (real client IP, not the proxy IP).\---## 💡 Future suggestions (not blocking)* **Substring search**: current search is prefix-only. If workers need to searchthe middle of a plate, add Algolia/Typesense or a normalized search field.* **Firestore backups**:`gcloud firestore backups schedules create --database='(default)' --recurrence=daily --retention=7d --project=ngewa-carwash`* **Uptime monitor**: point UptimeRobot/BetterStack at `https://<domain>/health`.* **Worker management**: workers/passwords are hardcoded in env — adding a workerneeds a redeploy. Consider a `users` collection if the team grows.* **APP\_VERSION**: bump `APP\\\\\\\\\\\\\\\_VERSION` (index.html) and `VERSION` (sw.js) togetheron each release so users get fresh builds and the install card shows the version.* **Deploy target**: repo is configured for **Railway** (`railway.json`,`Procfile`). `web: node server.js` also works on Render if you switch.\---## Files changed this session* `server.js` — finance, security, POS, search, prod hardening* `public/index.html` — XSS escaping, worker profile cards, install card, finance UI* `public/sw.js` — cache versioning + network-first HTML* `public/icon.png` — new PWA icon (was referenced but missing)* `scripts/cleanup-pos-expenses.js` — new one-time data cleanup (dry-run default)* `edoh.md` — this file